#include <stdio.h>

/**
 * main : Prints " with proper grammar, but the outcome is
 * a pieceof art," followed by a new line.
 * Return: Always 0.
 */

int main(int argc, char **argv)

{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}	
